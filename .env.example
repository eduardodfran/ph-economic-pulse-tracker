# Example environment variables for local reproduction. Copy to `.env` or export in your shell.
GCP_PROJECT=your-gcp-project-id
GOOGLE_APPLICATION_CREDENTIALS=/full/path/to/service-account.json
DBT_DATASET=ph_economy_staging
GCP_REGION=asia-southeast1
TF_VAR_project=your-gcp-project-id
TF_VAR_region=asia-southeast1
BUCKET_NAME=ph-economic-pulse-lake-eduardo
# Optional: set this if you keep a local profiles.yml inside the repo
DBT_PROFILES_DIR=
